// /home/vladimir/impress_server/applications/sqlproject/config/hosts.js

// Hosts (and IPs) array to be handled by application
// Wildcard "*" is allowed for masking random or empty substring

module.exports = [
	"*"
]
